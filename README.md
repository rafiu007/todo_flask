# todo_flask
install virtual environment
``` pip install --user virtualenv ```

Create a virtual env
```virtualenv -p python3 env ```

Install Flask
``` pip install flask ```

Run Server
``` python server.py```
